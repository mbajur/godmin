<% if field.belongs_to? %>
  <%= f.select field.select_attribute, field.collection, {}, data: { controller: "select-box" } %>
<% elsif field.many_to_many? || field.has_many? %>
  <%= f.select field.select_attribute, field.collection, { label: field.attribute.to_s.humanize }, multiple: true, data: { controller: "select-box" } %>
<% else %>
  <%= f.text_field field.attribute %>
<% end %>
